{
  "name": "habit-tracker-workspace",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "setup": "npm --prefix backend install && npm --prefix frontend install",
    "start:backend": "npm --prefix backend run start",
    "start:frontend": "npm --prefix frontend run start",
    "start:frontend:clear": "npm --prefix frontend run start:clear",
    "test": "npm --prefix backend run test && npm --prefix frontend run test"
  }
}
